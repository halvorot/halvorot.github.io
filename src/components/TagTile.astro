---
interface Props {
  text: string;
  classes?: string;
}

const { text, classes, ...rest } = Astro.props;
---

<div
  class={`text-slate-200 text-sm px-2 rounded-md bg-black bg-opacity-50 ${classes}`}
  {...rest}
>
  {text}
</div>
